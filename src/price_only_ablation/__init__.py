"""Price-only ablation package.

This package contains a standalone pipeline for fair comparison with the
hybrid model by reusing the same price LSTM + company embedding + ANN design
without the sentiment branch.
"""
